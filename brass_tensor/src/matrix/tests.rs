#[cfg(test)]

#[test]
fn default_int() {
    // use super::Matrix;
    // let width = 4;

    // let matrix = Matrix::diaganol_default(1, width, width);
    // for row in 0..width {
    //     for col in 0..width {
    //         let val = *matrix.at()
    //         if row == col {

    //         }
    //         else {

    //         }
    //     }
    // }
}